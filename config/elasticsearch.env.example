# Elasticsearch Configuration
# Copy this file to elasticsearch.env and adjust values as needed

# Discovery type (single-node for development, multi-node for production)
discovery.type=single-node

# Security settings (disable for development)
xpack.security.enabled=false

# Java heap size (adjust based on available memory)
ES_JAVA_OPTS=-Xms1g -Xmx1g

